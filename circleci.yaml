deployment:
  dockerhub:
    branch: master
    commands:
      - $DOCKER_HUB_TRIGGER

dependencies:
  override:
    - sudo pip install --upgrade docker-compose==1.3.3

test:
  override:
    - docker-compose run -d --no-deps web
    - mocha